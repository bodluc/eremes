    $(document).ready(function(){
        $('#slider3').plusSlider({
            sliderEasing: 'easeInOutQuart', // Anything other than 'linear' and 'swing' requires the easing plugin
            fullWidth: 1,
            //width:1280,
            sliderType: 'slider', // Choose whether the carousel is a 'slider' or a 'fader'
            speed: 2000,
            displayTime: 4000         
        });

    });